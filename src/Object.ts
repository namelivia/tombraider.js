class Model {
    constructor(name:string , action:string , params:string, model:string) {
        this.name = name
        this.action = action
        this.params = params
        this.model = model
    }
}
